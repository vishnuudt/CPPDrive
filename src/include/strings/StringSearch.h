#pragma once

#include <string>

using namespace std;

namespace Drive::strings{

    class StringSearch{
        public:
            bool search(string one, string two);

    };


    class KnuthMorrisPratt{
        public:
            bool search(string one, string two);


    };
}